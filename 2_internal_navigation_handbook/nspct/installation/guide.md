# NSPCT INSTALLATION GUIDE VIA CURL
## STEPS
- run in terminal:
````MARKDOWN
cd "$(mktemp -d)" && curl -L "https://dev-nspct.pantheonsite.io/sites/default/files/binaries/nspct" --output nspct && chmod +x nspct && sudo mv nspct /usr/local/bin/
````
